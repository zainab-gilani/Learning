# Not readable
# No way to discover what the file or many classes are doing
# Everything in a single file means any change you make affects the "change history" of the ENTIRE file
# Your file may in fact contain different, conflicting even, groups of functionality all jumbled in a single place

from mysillymath import *

sum = add(1, 2)
print(sum)